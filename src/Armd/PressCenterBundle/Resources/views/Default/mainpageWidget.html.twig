{% spaceless %}
    {% if items is not empty %}
        {# todo: html #}
        <div id="press_center_block" style="margin: 20px 0; padding-left:20px; border: 1px solid gray;">
            <div style="font-size:20px;">
                ПРЕСС ЦЕНТР
                <span style="margin-left:30px;">сегодня, {{ 'now'|date('d F') }}</span>
            </div>
            <div id="press_center_widget" class="carousel" style="margin-top:20px;">
            {% for item in items %}
                <div><a href="{{ path('armd_press_center_item', {slug: item.slug}) }}">{{ item.title }}</a></div>
            {% endfor %}
            </div>

            <a href="{{ path('armd_press_center_home') }}">Перейти в раздел</a>
        </div>
    {% endif %}
{% endspaceless %}