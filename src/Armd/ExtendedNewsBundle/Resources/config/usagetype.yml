parameters:
    armd_news.usagetypes:
        types:
            item:
                controller: news
                action: item                
                route:
                    pattern: /item-{id}
                    requirements:
                        id: \d+
            list:
                controller: news
                action: list
                route:
                    pattern: /{page}
                    defaults:
                        page: 1
                    requirements:
                        page: \d+
                params:
                    per_page:
                        title: Количество элементов на странице
                        type: int                        
                        default: 10
                        requirements: 1
        group:
            list:
                types: [list]
            listitem:
                types: [item, list]

services:
    armd_news.usagetype:
        class: %armd_cms.usagetype.class%
        arguments: ["armd_news", %armd_news.usagetypes%, "ArmdExtendedNewsBundle"]
        tags:
            -  { name: armd_cms.usage }