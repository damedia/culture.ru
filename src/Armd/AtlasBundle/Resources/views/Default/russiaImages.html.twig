{% extends ':Frontend:base.html.twig' %}

{% block title %}
    {{ parent() }} &mdash; {{ 'Navigation.Images of Russia.Title'|trans }}
{% endblock %}

{% block page_header_title %}
    {{ 'Navigation.Images of Russia.Title'|trans }}
{% endblock %}

{% block page_header_search %}
    {% spaceless %}
        <div class="category-chooser" id="category-chooser">
            <a href="#plitka" class="plitka-icon active" title="Плитка">Плитка</a>
            <a href="#full-list" class="full-list-icon" title="Полный список">Полный список</a>
            <a href="#short-list" class="short-list-icon" title="Краткий список">Краткий список</a>
        </div>
        <div class="search-category">
            <span class="search-dates-label">Выбрать</span>

            <div class="search-category-selects">
                <select name="region" id="region" class="uni">
                    <option value="">Регион</option>
                    {% for region in regions %}
                        <option value="{{ region.id }}">{{ region.title }}</option>
                    {% endfor %}
                </select>
                <select name="object-type" id="select2" class="uni">
                    <option value="">Тип</option>
                    {% for type in types %}
                        <option value="{{ type.id }}">{{ type.title }}</option>
                    {% endfor %}
\               </select>
                <select name="select3" id="select3" class="uni">
                    <option value="">Тематика</option>
                    {% for thematic in thematics %}
                        <option value="{{ thematic.id }}">{{ thematic.title }}</option>
                    {% endfor %}
\                </select>
            </div>
            <a href="#" class="button search-dates-button">Показать</a>
        </div>
    {% endspaceless %}
{% endblock %}

{% block page_header_search_scope %}
    <div class="search-checkboxes">
        <label><input type="checkbox" name="check1" id="check1"/><span>Искать в текущем разделе</span></label>
    </div>
{% endblock %}

{% block body %}

    {#<div class="blocksection index-blocksection w clearfix" id="f-section">#}
        {#<div class="title-block">#}
            {#<h2>{{ 'Navigation.Images of Russia.Title'|trans }}</h2>#}
        {#</div>#}

        {#<div class="obrazy">#}
            {#<form action="" class="obr-form">#}
                {#<div class="obr-text-input">#}
                    {#<input type="text" name="search" id="search_text" value="{{ 'russia_images.search'|trans }}"/>#}
                {#</div>#}
                {#<input type="button" class="obr-submit-input" value=""/>#}
            {#</form>#}
            {#<div id="images-of-russia-wrapper">#}
                {#<div id="images-of-russia-loading" class="loading-block"></div>#}
                {#<div id="images-of-russia-container">#}
                    {#{% render 'ArmdAtlasBundle:Default:russiaImagesList' %}#}
                {#</div>#}
            {#</div>#}
        {#</div>#}
    {#</div>#}

{% endblock %}

{% block javascripts %}
    {{ parent() }}

    <script type="text/javascript" src="{{ asset('bundles/armdatlas/js/russia_images.js') }}"></script>
    <script type="text/javascript">
        $(function () {
            {#armdRussiaImages.init();#}
            {#armdRussiaImages.listUrl = '{{ path('armd_atlas_russia_images_list') }}';#}
        });
    </script>
{% endblock %}