{% if options.src %}
    <iframe
            src="http://cloud.tvigle.ru/video/{{ options.videoId }}/?width={{ options.width }}&height={{ options.height }}"
            width="{{ options.width }}"
            height="{{ options.height }}"
            frameborder="{{ options.frameborder }}"
            scrolling="no"
            webkitallowfullscreen=""
            mozallowfullscreen=""
            allowfullscreen="">
    </iframe>
{% else %}
    {{ options.embed_html|raw }}
{% endif %}