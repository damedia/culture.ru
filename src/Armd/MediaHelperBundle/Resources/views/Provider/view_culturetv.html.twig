<iframe
    id="{{ options.id }}"
    src="{{ media.providerMetadata['player_url'] }}"
    width="{{ options.width }}"
    height="{{ options.height }}"
    frameborder="{{ options.frameborder }}">
</iframe>