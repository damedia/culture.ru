<article class="chronicles-event" id="event_{{ event.id }}">
	<span class="chronicles-date" title="{{ 'year'|trans }}">{{ event.date|date('Y')|abs }} {{ 'year'|trans }}</span>
	<div class="chronicles-event-text">
		<h1>{{ event.title }}</h1>
		{{ event.body|raw }}
	</div>
</article>

